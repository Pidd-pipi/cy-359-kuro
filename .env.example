# Compose 项目名（小写字母/数字/-/_），避免中文目录名导致 project name 为空
COMPOSE_PROJECT_NAME=lporienteering

DB_NAME=app
DB_USER=app
DB_PASSWORD=app_pwd
DB_ROOT_PASSWORD=root_pwd

JWT_SECRET=change_me_to_a_long_random_string

# 宿主机映射端口（容器内分别固定为 前端 80 / 后端 29519 / 数据库 5432）
FRONTEND_PORT=28519
BACKEND_PORT=29519
DB_PORT=5432
